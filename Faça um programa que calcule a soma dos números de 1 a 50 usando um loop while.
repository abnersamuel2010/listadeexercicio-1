#include <stdio.h>
int main() {
    int n = 1;
    int valor = 0;
    
    while ( n <= 50) {
    soma +- n;]
    n++;
    }
    
    printf("A soma dos numeros de 1 a 50 é: %d\n", soma);

    return 0;
}
